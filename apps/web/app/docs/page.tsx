import Link from 'next/link';
import CopyCommand from '@/components/CopyCommand';
export const metadata = { title: 'Run locally — Breakroom docs' };
export default function DocsPage() {
  return <div className="wrap docs-page"><aside className="docs-nav"><span className="eyebrow">DOCUMENTATION</span>{[['quickstart','Quickstart'],['adapter','Your agent'],['cli','CLI & artifacts'],['authoring','Custom Fire Drills'],['ci','Release Checks'],['verdicts','Verdict meanings'],['privacy','Privacy & execution'],['team','Team reporting'],['limits','Limits & troubleshooting']].map(([id,label]) => <a key={id} href={'#'+id}>{label}</a>)}</aside><article className="docs-content"><span className="eyebrow">LOCAL FIRST. EVIDENCE ALWAYS.</span><h1>Enter the breakroom<span>.</span></h1><p className="lead">Reproduce a failure, understand the effects, then make it a test your agent can pass.</p>
    <section id="quickstart"><h2>01 / Run the local demo</h2><p>From this repository, use Python 3.11–3.14 on macOS or Linux. The core needs no runtime services, Docker, signup, model key, or real payment credentials.</p><CopyCommand command={'python3 -m venv venv\nsource venv/bin/activate\npython -m pip install -e ./packages/breakroom-core\nbreakroom doctor\nbreakroom demo --output ./artifacts/demo'} /><p>This installs the source in this checkout. Do not install an unrelated public package with the same name. The tutorial exits 0 when the faulty reference fails and the corrected reference passes; that is not a passing release gate for the faulty agent.</p><p>Open <code>artifacts/demo/report.html</code> for recorded evidence. The JSON, JUnit, comparison, and regression export sit alongside it.</p></section>
    <section id="adapter"><h2>02 / Connect your agent</h2><p>The supported Python contract is <code>run(task, tools, context) → AgentResult</code>. The complete offline example maps application operations to TestPay and TestDesk for the five initial drills. The full reference also demonstrates grouped requests and event handling.</p><CopyCommand command={'cd examples/customer-adapter\nbreakroom run --agent adapter:run --pack support-refunds --case normal-refund --case refund-response-lost --case refund-before-commit --case ticket-write-failure --case similar-customers --out artifacts/customer'} /><p>The task supplies authenticated requester data, known references, integer money and policy. Context supplies a logical operation ID, deadline, cancellation, and virtual sleep. It does not reveal future faults or expected answers.</p><p>Inject the fake tools into your application. Preserve the same operation key across retries of one logical request; give distinct authorized requests distinct keys. Return structured claims and the evidence references obtained from tool responses. Free text alone is unknown evidence.</p><p>Supported claims include <code>refund_succeeded</code>, <code>refund_pending</code>, <code>refund_failed</code>, <code>ticket_updated</code>, and <code>escalated</code>. This adapter is a concrete Python integration, not universal framework support.</p></section>
    <section id="cli"><h2>03 / Inspect, compare, export</h2><p>Run these from the repository root. A faulty suite is expected to return exit 1.</p><CopyCommand command={'breakroom run --agent examples.agents.faulty:run --pack support-refunds --out ./artifacts/baseline\nbreakroom run --agent examples.agents.corrected:run --pack support-refunds --out ./artifacts/candidate\nbreakroom compare ./artifacts/baseline ./artifacts/candidate\nbreakroom export-case ./artifacts/baseline --case refund-response-lost --out ./regressions\npython ./regressions/test_regression.py --agent examples.agents.corrected:run'} /><p>Each bundle includes JSON, escaped standalone HTML, and JUnit XML. An exported test executes a new isolated trial against its frozen manifest. Keep its matching local core version; it is not a screenshot or prerecorded outcome.</p><p><code>--trials 3</code> repeats each case with the same reviewed seed; reports retain the actual trial counts. Use <code>--seed 1</code> or <code>--seed 2</code> for the other reviewed deterministic fixtures; seed 0 preserves the canonical amounts and identities. Reports freeze the source manifest, materialized fixture and generator version. These variations preserve each drill’s business relationships; they do not make a network-backed model deterministic. Comparison requires matching case, fixture, oracle and simulator contracts. Unknown model versions, costs and token counts stay unknown.</p></section>
    <section id="authoring"><h2>04 / Author bounded scenario data</h2><p>Copy a JSON case from <code>scenario-packs/support-refunds</code> into a private local directory. Keep the full schema, version, provenance, fixture, faults, limits, and allowlisted assertions.</p><CopyCommand command="breakroom validate-pack ./scenario-packs/support-refunds" /><p>Only bounded JSON is supported in this release. YAML and executable assertion expressions are not accepted. Faults distinguish before-write refusal, after-commit response loss, and read-response changes. Review the exact supported actions in the engine; a syntactically valid manifest still needs executed negative and positive controls.</p></section>
    <section id="ci"><h2>05 / Make a Release Check</h2><p>Install the local core in your own CI and run your adapter. Retain the report bundle and publish its JUnit file in your CI tool. No hosted worker is installed or enabled for you.</p><CopyCommand command="breakroom run --agent examples.agents.corrected:run --pack support-refunds --out artifacts/release" /><p>Exit <strong>0</strong>: all required checks passed. Exit <strong>1</strong>: at least one known failure. Exit <strong>2</strong>: invalid configuration, missing coverage, unsupported mandatory behavior, unknown evidence, or infrastructure error without a known failure. Zero executed tests cannot pass.</p><CopyCommand command="breakroom mutation-check --pack support-refunds --seed 0 --seed 1 --seed 2 --trials 3 --out artifacts/coverage" /><p>A mutation review executes corrected and deliberately faulty controls. It only counts a detection when the relevant checks fail and the intended challenge was exercised. Its exit 0 validates the drill’s detection ability, not the faulty agent’s release readiness.</p><p>Use the included local workflow as a starting point. No remote CI run has been claimed or enabled by this build.</p></section>
    <section id="verdicts"><h2>06 / Read the verdict precisely</h2><div className="verdict-definition"><strong>PASS</strong><p>The required assertions passed under these modeled conditions.</p><strong>FAIL</strong><p>A known state, policy, outcome or evidence check failed. A known critical failure takes precedence over missing evidence.</p><strong>INCONCLUSIVE</strong><p>Required evidence, execution, or fault coverage is unknown. This blocks a successful release check.</p><strong>UNSUPPORTED</strong><p>A mandatory adapter capability is unavailable. It was not tested successfully.</p></div><p>Execution status is separate: completed, errored, timed out, or cancelled. Safety, completion, evidence and coverage checks remain separate. An escalation can only satisfy a drill that explicitly permits it.</p></section>
    <section id="privacy"><h2>07 / Know where code runs</h2><p>Customer code executes on your machine or CI. Imports execute trusted Python; a subprocess is not a security sandbox. The injected tools change only synthetic state, but your own code can independently access files or networks.</p><p>Default workers do not inherit service secrets. Passing selected model-provider configuration requires explicit local opt-in through <code>--allow-network --env VARIABLE_NAME</code>. Do not pass payment or help-desk credentials. See the optional no-network container in <code>docs/local-runner.md</code>.</p><p>The demo API runs only fixed reference agents and cases. Demo evidence expires after 30 minutes or API restart; browser recordings stay on this browser until cleared. Private projects accept separate, explicit minimized uploads. Imported evidence is labeled customer-generated; its checksum does not certify the execution.</p></section>
    <section id="team"><h2>08 / Review together</h2><p>Start the optional local team services from the repository root, then open <Link href="/projects">Team projects</Link> at <code>http://127.0.0.1:3000</code>.</p><CopyCommand command="docker compose -f infra/compose.yaml -f infra/compose.team.yaml up -d --build --wait" /><p>The local setup uses a clearly labeled development identity and PostgreSQL. Public operation requires a configured OIDC provider and HTTPS. The development identity is prohibited in production.</p><CopyCommand command="breakroom prepare-upload ./artifacts/candidate --case refund-response-lost --out ./artifacts/prepared-report.json" /><p>Review the prepared JSON before selecting and uploading it. Automated redaction removes raw messages, contacts, source paths and unrestricted payloads, but it is imperfect. Retain the private <code>.breakroom/redaction.key</code> locally so baseline and candidate identifiers remain comparable; never upload that key.</p><p>Owners manage members, scoped API keys, retention and deletion. Developers upload reports and manage private suite metadata; viewers read authorized evidence. Reports expire automatically. Deleting a project removes its active reports and associated records; operator-managed backups follow a separate retention policy.</p><p>For customer-owned CI, use the exact commands and inactive workflow example in <code>docs/customer-worker.md</code>. Nothing runs or uploads on a schedule until the owner enables their reviewed workflow.</p></section>
    <section id="limits"><h2>09 / Scope & troubleshooting</h2><p>TestPay and TestDesk are original, limited fake services. The cases are synthetic. No external payment provider has been sandbox-validated, and no commercial model is benchmarked here. Passing tests do not establish general agent safety.</p><p><strong>API offline?</strong> Start the local API using <code>apps/api/README.md</code>, then retry the run. <strong>Expired demo?</strong> Execute a new run; a browser recording is only saved evidence. <strong>Unknown claims?</strong> Map real tool responses to structured claims and preserve their evidence references.</p><p>Windows users can use a Linux container or WSL. The process runner relies on POSIX process groups. The local pack contains 24 drills. Private team reporting is available in the optional local stack. A public hosted service and live checkout are not configured.</p><Link className="button accent" href="/demo">Try the live built-in demo ↗</Link></section>
  </article></div>;
}
