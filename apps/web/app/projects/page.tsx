import TeamHome from '@/components/Team';
export const metadata = { title: 'Your workspace — Breakroom', robots: { index: false, follow: false } };
export default function ProjectsPage() { return <TeamHome />; }
